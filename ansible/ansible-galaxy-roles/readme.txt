Open a shell prompt (Terminal app on a Mac) and cd into the folder containing the Vagrantfile
Run the following command to install the necessary Ansible roles for this profile: 
$ott> sudo ansible-galaxy install -r ansible/ansible-galaxy-roles/required.roles --ignore-errors